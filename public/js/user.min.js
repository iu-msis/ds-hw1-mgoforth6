var userApp = new Vue({
    el: '#user',
    data: {
      person: {
        name : '',
        country : '',
        birthdate : '',
        age : '',
        email : '',
        picture : ''
      }
    },
    computed: {
      dob: function() {
        return moment(this.person.birthdate).diff(moment(), 'years')
      }
    },
    methods: {
      fetchPerson () {
      fetch('https://randomuser.me/api')
      .then( response => response.json() )
          .then( json => {userApp.person = json} )
          .catch( err => {
            console.log('USER FETCH ERROR:');
            console.log(err);
    }}},
    created () {
    this.fetchPerson();
  }})
