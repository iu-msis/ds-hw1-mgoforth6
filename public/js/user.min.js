var userApp = new Vue({
    el: '#user',
    data: {
      results: {
        name : '',
        location : '',
        birthdate : '',
        age : '',
        email : '',
        picture : ''
      }
    },
    computed: {
      dob: function() {
        return moment(this.person.birthdate).diff(moment(), 'years')
      }
    },
    methods: {
      fetchResults () {
        $.ajax({
    url: 'https://randomuser.me/api/',
    dataType: 'json',
    success: function(data) {
      console.log(data);
    }
  });
    }},
    created () {
    this.fetchPerson();
  }})
